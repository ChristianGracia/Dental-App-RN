import React, { Component } from "react";
import {
  View,
  TouchableOpacity,
  ScrollView,
  Text,
  StyleSheet,
  TextInput
} from "react-native";

class Blog extends Component {
  render() {
    return <Text>Hi</Text>;
  }
}

const styles = StyleSheet.create({
  container: {
    backgroundColor: "#fff"
  }
});

export default Blog;
